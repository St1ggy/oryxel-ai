{
  "private": true,
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "dev": "bun run --cwd apps/web dev",
    "build": "bun run --cwd apps/web build",
    "check": "bun run --cwd apps/web check",
    "lint": "bun run --cwd apps/web lint && bun run --cwd apps/worker lint && bun run --cwd packages/ai lint && bun run --cwd packages/db lint",
    "format": "bun run --cwd apps/web format && bun run --cwd apps/worker format && bun run --cwd packages/ai format && bun run --cwd packages/db format",
    "db:push": "bun run --cwd packages/db db:push",
    "db:push:prod": "bun run --cwd packages/db db:push:prod",
    "db:generate": "bun run --cwd packages/db db:generate",
    "db:migrate": "bun run --cwd packages/db db:migrate",
    "db:studio": "bun run --cwd packages/db db:studio"
  },
  "dependencies": {
    "drizzle-orm": "^0.45.1",
    "zod": "^4.3.6"
  },
  "overrides": {
    "drizzle-orm": "0.45.2",
    "zod": "4.3.6"
  }
}
